<nav class="navbar navbar-expand-md navbar-light bg-light shadow-sm sticky-top border-bottom">
  <div class="container">
    <!-- Logo and Explore Listings Link -->
    <a class="navbar-brand fw-bold" href="/listings">
      <i class="fa-regular fa-compass me-1"></i> 
    </a>

    <!-- Navbar Toggler for Mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <!-- Left Links -->
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link" href="/listings">Explore</a>
        </li>

        <% if (currentUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/listings/new">Add Listing</a>
          </li>
          <% } %>
      </ul>

      <div class="container my-3">
        <div class="d-flex justify-content-center align-items-center gap-4">
            <!-- Search Input -->
            <form action="/listings/search" method="GET" class="d-flex w-75">
                <input type="text" name="query" class="form-control" placeholder="Search listings..." aria-label="Search listings" />
                <button type="submit" class="btn btn-primary ms-2">Search</button>
            </form>
        </div>
    </div>
    
      <!-- Right Links (User specific links) -->
      <ul class="navbar-nav ms-3 mb-2 mb-md-0">
        <% if (!currentUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/signup">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login">Log In</a>
          </li>
          <% } else { %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown"
                aria-expanded="false">
                Hello, <%= currentUser.username %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                <li><a class="dropdown-item" href="/listings/new">Add Listing</a></li>
                <li><a class="dropdown-item" href="/logout">Log Out</a></li>
              </ul>
            </li>
            <% } %>
      </ul>
    </div>
  </div>
</nav>